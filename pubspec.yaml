name: follow
description: Follow App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  font_awesome_flutter:
  url_launcher:
  http:
  webview_flutter:
  receive_sharing_intent:
  connectivity_plus:
  fluttertoast:
  sqflite:
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:

flutter:
  uses-material-design: true
  assets:
    - assets/